function [x, y, z] = sph2cart2(theta, phi, rho)
% TODO: convert 3d vector from spherical coordinates into [x(j), y(j), z(j)] 
% Use convention such that theta in [0,pi], phi in [0,2pi] and rho>=0.
